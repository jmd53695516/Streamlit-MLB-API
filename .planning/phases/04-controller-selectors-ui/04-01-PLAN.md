---
phase: 04-controller-selectors-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mlb_park/services/mlb_api.py
  - src/mlb_park/controller.py
  - src/mlb_park/pipeline/__init__.py
  - tests/controller/__init__.py
  - tests/controller/conftest.py
  - tests/controller/test_view_model.py
  - tests/services/test_team_hitting_stats.py
  - tests/fixtures/team_stats_empty.json
  - tests/fixtures/team_stats_all_pitchers.json
  - tests/fixtures/team_stats_zero_hr_player.json
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "A cached services wrapper `get_team_hitting_stats(team_id, season)` exists and returns hydrated roster entries with per-player HR totals."
    - "A `ViewModel` frozen dataclass is defined in `src/mlb_park/controller.py` matching D-06 schema exactly."
    - "`ViewModel.to_dict()` produces a JSON-safe dict (round-trips through `json.dumps`)."
    - "`load_parks` is re-exported via `mlb_park.pipeline` so Phase 4 honors D-02 single-import-origin."
    - "Controller test package exists (`tests/controller/`) with a `StubAPI` extended to serve `get_teams` and `get_team_hitting_stats`."
    - "Three synthetic team-stats fixtures exist for downstream filter/sort/empty-state tests."
  artifacts:
    - path: "src/mlb_park/services/mlb_api.py"
      provides: "get_team_hitting_stats wrapper + _raw_team_hitting_stats helper"
      contains: "get_team_hitting_stats"
    - path: "src/mlb_park/controller.py"
      provides: "ViewModel dataclass + to_dict()"
      contains: "class ViewModel"
    - path: "src/mlb_park/pipeline/__init__.py"
      provides: "re-export of load_parks (D-02 alignment)"
      contains: "load_parks"
    - path: "tests/controller/conftest.py"
      provides: "extended StubAPI, fixture loaders"
      contains: "get_team_hitting_stats"
    - path: "tests/controller/test_view_model.py"
      provides: "JSON round-trip test"
    - path: "tests/services/test_team_hitting_stats.py"
      provides: "services wrapper unit test"
    - path: "tests/fixtures/team_stats_empty.json"
      provides: "D-15 empty-roster fixture"
    - path: "tests/fixtures/team_stats_all_pitchers.json"
      provides: "All-pitchers edge fixture"
    - path: "tests/fixtures/team_stats_zero_hr_player.json"
      provides: "0-HR player fixture"
  key_links:
    - from: "src/mlb_park/controller.py"
      to: "mlb_park.pipeline"
      via: "imports HREvent, HitData, VerdictMatrix, PipelineError, CURRENT_SEASON, load_parks"
      pattern: "from mlb_park.pipeline import"
    - from: "tests/controller/conftest.py"
      to: "tests/pipeline/conftest.py::StubAPI"
      via: "subclass or extend the Phase 3 StubAPI pattern"
      pattern: "class StubAPI"
---

<objective>
Land the type-level foundations for Phase 4: the new services wrapper
`get_team_hitting_stats` (D-11 amended endpoint), the `ViewModel` frozen
dataclass (D-06), and the controller test scaffolding per VALIDATION.md Wave 0.

Do NOT implement `build_view` or helper functions yet — those are Wave 2
(Plan 04-02). This plan freezes the contracts that Wave 2 implements against
and Wave 3 wires into Streamlit.

Purpose: Interface-first ordering — Wave 2's `build_view` depends on the
ViewModel shape and the new services wrapper; landing them first means Wave 2
can be implemented without re-reading the controller file.

Output: Services wrapper + ViewModel skeleton + test infra + 3 new fixtures +
`load_parks` re-export, all committed with green tests.

NOTE: This plan is pure infrastructure (types, wrapper, test scaffolding). It
lands ZERO user-visible UX functionality — UX-01 (team sort) and UX-02 (hitter
sort/filter) are delivered in Plan 04-02's helpers + Plan 04-03's selectboxes,
which is why `requirements:` is empty here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-controller-selectors-ui/04-CONTEXT.md
@.planning/phases/04-controller-selectors-ui/04-RESEARCH.md
@.planning/phases/04-controller-selectors-ui/04-VALIDATION.md
@CLAUDE.md
@src/mlb_park/services/mlb_api.py
@src/mlb_park/pipeline/__init__.py
@src/mlb_park/geometry/park.py
@src/mlb_park/geometry/verdict.py
@tests/pipeline/conftest.py
@tests/fixtures/team_stats_147_2026.json

<interfaces>
<!-- Pipeline public surface (D-02: single import origin) -->
From src/mlb_park/pipeline/__init__.py (POST this plan's Task 2):
```python
# Phase 4 imports ONLY these names from mlb_park.pipeline:
extract_hrs, hr_event_to_hit_data, HREvent, PipelineResult, PipelineError,
HitData, compute_verdict_matrix, load_all_parks, load_parks,
MLBAPIError, CURRENT_SEASON
```

From src/mlb_park/geometry/verdict.py (verified shape):
```python
@dataclass(frozen=True)
class VerdictMatrix:
    hrs: tuple[HitData, ...]
    parks: tuple[Park, ...]
    venue_ids: np.ndarray        # shape (n_parks,)
    cleared: np.ndarray          # shape (n_hrs, n_parks) bool
    fence_ft: np.ndarray         # shape (n_hrs, n_parks) float
    margin_ft: np.ndarray        # shape (n_hrs, n_parks) float
    # ... (no .records attribute; iteration via .iter_records())
    def iter_records(self) -> Iterator[VerdictRecord]: ...
    def parks_cleared(self, hr_index: int) -> int: ...
```

From src/mlb_park/geometry/park.py:
```python
def load_parks(venues: dict[int, dict]) -> dict[int, Park]: ...
```

<!-- Existing services pattern (mirror this) -->
From src/mlb_park/services/mlb_api.py:
- Each public wrapper has a `_raw_<name>` sibling that returns the raw JSON.
- `@st.cache_data(ttl=...)` decorates the PUBLIC wrapper only.
- `MLBAPIError` is raised on HTTP failures; the module uses `requests` directly.
</interfaces>

<constraints>
- Direct HTTP to statsapi.mlb.com/api/v1 only. No third-party wrappers.
- `@st.cache_data` lives at the services boundary (D-01, D-14).
- Controller module MUST NOT import `streamlit` (D-23). Enforce via test in Plan 04-02.
- Public import origin: `from mlb_park.pipeline import ...` only (D-02).
- Commit with `git commit --no-verify` (Phase 3 pattern — pre-commit hooks run full suite; this plan's tests gate at task level).
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_team_hitting_stats services wrapper (D-11 amended)</name>
  <files>
    src/mlb_park/services/mlb_api.py,
    tests/services/test_team_hitting_stats.py
  </files>
  <behavior>
    - `_raw_team_hitting_stats(team_id, season)` calls
      `GET /teams/{team_id}/roster?rosterType=active&hydrate=person(stats(type=statsSingleSeason,season={season},group=hitting))`
      via the existing `_get_json` helper; raises `MLBAPIError` on HTTP failure.
    - `get_team_hitting_stats(team_id, season)` is a public wrapper decorated
      `@st.cache_data(ttl="1h")` (per D-14) that returns the `roster` list
      (`response["roster"]` — list[dict]). Empty roster → `[]` (D-15).
    - Signature: `def get_team_hitting_stats(team_id: int, season: int) -> list[dict]`.
    - Unit test (`tests/services/test_team_hitting_stats.py`):
      1. `test_returns_roster_list` — monkeypatch `_raw_team_hitting_stats` to
         return the existing `tests/fixtures/team_stats_147_2026.json` payload;
         assert return value is the `roster` list (len > 0, dict entries).
      2. `test_empty_roster_returns_empty_list` — monkeypatch to return
         `{"roster": []}`; assert return is `[]`.
      3. `test_raw_helper_builds_expected_url` — monkeypatch the HTTP layer
         (`_get_json`) and assert the URL contains
         `teams/147/roster`, `rosterType=active`, `hydrate=person(stats(type=statsSingleSeason,season=2026,group=hitting))`.
  </behavior>
  <action>
    Follow the existing `_raw_*` / public-wrapper pattern in
    `src/mlb_park/services/mlb_api.py` (mirror `get_game_log` / `_raw_game_log`
    structure). The URL is from D-11 AMENDED (confirmed by RESEARCH.md Q1 and
    the existing `tests/fixtures/team_stats_147_2026.json`). Do NOT re-query the
    endpoint — consume the existing fixture for the URL-shape assertion.

    If the existing file already provides a shared `_get_json` / requests helper,
    reuse it. Otherwise match the request idiom used by `get_game_log` verbatim.

    Cache TTL string: `"1h"` (D-14, matches gameLog).
  </action>
  <verify>
    <automated>pytest tests/services/test_team_hitting_stats.py -q</automated>
  </verify>
  <done>
    get_team_hitting_stats exists in services/mlb_api.py with @st.cache_data(ttl="1h");
    all three unit tests pass; no changes to other existing wrappers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ViewModel frozen dataclass + to_dict() + load_parks re-export</name>
  <files>
    src/mlb_park/controller.py,
    src/mlb_park/pipeline/__init__.py,
    tests/controller/__init__.py,
    tests/controller/test_view_model.py
  </files>
  <behavior>
    - `ViewModel` is a `@dataclass(frozen=True)` with every field from D-06
      (verbatim names and types):
      season, team_id, team_abbr, player_id, player_name, venue_id,
      venue_name, player_home_venue_id, events (tuple[HREvent, ...]),
      plottable_events (tuple[HREvent, ...]),
      verdict_matrix (VerdictMatrix | None),
      clears_selected_park (tuple[bool, ...]),
      totals (dict[str, int | float]),
      errors (tuple[PipelineError, ...]).
    - `ViewModel.to_dict(self) -> dict` returns a JSON-safe dict:
      - dataclasses converted via `asdict`-style traversal
      - `events` and `plottable_events` → list[dict] (HREvent fields; `game_date`
        (datetime.date) → ISO string via `.isoformat()`; other fields dumped as-is)
      - `verdict_matrix` → RESEARCH.md-prescribed summary dict (D-24 scope stays
        in controller.py — do NOT add a `to_dict()` method to VerdictMatrix):
        ```python
        {
          "shape": [n_hrs, 30],   # list(self.verdict_matrix.cleared.shape)
          "venue_ids": [...],      # self.verdict_matrix.venue_ids.tolist()
          "cleared_per_park": {    # {venue_id: int_count_of_plottable_hrs_that_cleared}
              int(vid): int(self.verdict_matrix.cleared[:, j].sum())
              for j, vid in enumerate(self.verdict_matrix.venue_ids.tolist())
          },
        }
        ```
        When `verdict_matrix is None`, emit JSON `null`.
      - `PipelineError` entries → `{"game_pk": int|None, "endpoint": str, "message": str}`
        (fields verified in `src/mlb_park/pipeline/events.py`).
      - `tuple` → `list`; top-level dict is fully JSON-serializable.
    - Test (`tests/controller/test_view_model.py`):
      1. `test_to_dict_json_safe` — construct a minimal ViewModel (no events, no
         matrix, empty errors) and assert `json.dumps(vm.to_dict())` does not
         raise.
      2. `test_to_dict_includes_all_fields` — assert the returned dict has keys
         matching every D-06 field name.
      3. `test_frozen` — assert `vm.season = 9999` raises `FrozenInstanceError`.
      4. `test_to_dict_verdict_matrix_summary_shape` — build a real
         `VerdictMatrix` (via `compute_verdict_matrix` with 2 synthetic HitData
         entries against 30 parks from an existing Phase 3 parks fixture);
         assert `vm.to_dict()["verdict_matrix"]` has keys
         `{"shape", "venue_ids", "cleared_per_park"}`, `shape == [2, 30]`,
         `len(venue_ids) == 30`, `cleared_per_park` values are ints.
  </behavior>
  <action>
    **Step 1 — Re-export `load_parks` via `mlb_park.pipeline`.**

    Edit `src/mlb_park/pipeline/__init__.py`:
    - Add `from mlb_park.geometry.park import load_parks` next to the existing
      `from mlb_park.geometry.verdict import HitData, compute_verdict_matrix`.
    - Add `"load_parks"` to `__all__` alongside the existing geometry re-exports.

    Rationale: Phase 4 consumers (controller.build_view in Plan 04-02) need to
    convert `dict[int, dict]` → `dict[int, Park]` before calling
    `compute_verdict_matrix`. Re-exporting from the pipeline package preserves
    D-02 (single import origin) — the controller imports EVERYTHING from
    `mlb_park.pipeline`, never from `mlb_park.geometry.*`.

    **Step 2 — Create `src/mlb_park/controller.py`.**

    Imports MUST route through `mlb_park.pipeline` only (D-02):

    ```python
    from mlb_park.pipeline import (
        HREvent, PipelineError, HitData, compute_verdict_matrix,
        load_all_parks, load_parks, MLBAPIError, CURRENT_SEASON,
    )
    from mlb_park.geometry.verdict import VerdictMatrix  # type only; pipeline doesn't re-export the matrix type
    ```

    `VerdictMatrix` is imported from geometry for the type annotation only —
    it's a type reference, not runtime composition (D-02 spirit preserved).
    The runtime call path (`load_parks`, `compute_verdict_matrix`) goes through
    `mlb_park.pipeline`.

    Do NOT implement `build_view` or helpers in this plan — that is Plan 04-02.
    The module should contain: module docstring, imports, `ViewModel` dataclass,
    and `to_dict()`. Nothing else yet.

    `to_dict()` verdict_matrix summary uses the RESEARCH.md-prescribed shape
    (see `<behavior>`). Do NOT add `VerdictMatrix.to_dict()` to the geometry
    layer — D-24 scopes the JSON-serialization responsibility to the controller.

    `totals` Claude's-discretion (D-09 note): use plain `dict[str, int | float]`
    for JSON-serialization simplicity. Do NOT introduce a `Totals` dataclass.

    Create `tests/controller/__init__.py` (empty — marks package).
  </action>
  <verify>
    <automated>pytest tests/controller/test_view_model.py -q</automated>
  </verify>
  <done>
    `load_parks` re-exported via `mlb_park.pipeline`; ViewModel dataclass exists
    with all D-06 fields; to_dict() round-trips through json.dumps and produces
    the RESEARCH-prescribed verdict_matrix summary shape; frozen semantics
    enforced; no build_view defined.
  </done>
</task>

<task type="auto">
  <name>Task 3: Scaffold tests/controller/conftest.py + 3 synthetic fixtures</name>
  <files>
    tests/controller/conftest.py,
    tests/fixtures/team_stats_empty.json,
    tests/fixtures/team_stats_all_pitchers.json,
    tests/fixtures/team_stats_zero_hr_player.json
  </files>
  <action>
    **Step 1 — Extend StubAPI for controller tests.**

    Create `tests/controller/conftest.py`. Define a `ControllerStubAPI` that
    composes or subclasses the Phase 3 `StubAPI` from `tests/pipeline/conftest.py`.
    Add these attributes/methods on top of the Phase 3 surface:

    - `MLBAPIError` (inherit from parent)
    - `get_game_log(person_id, season)` (inherit)
    - `get_game_feed(game_pk)` (inherit)
    - `load_all_parks()` (inherit)
    - `get_teams()` → returns canned list of team dicts (constructor arg `teams`)
    - `get_team_hitting_stats(team_id, season)` → returns canned list (constructor
      arg `team_hitting_stats: dict[int, list[dict]]` keyed by team_id)

    Re-use the Phase 3 `StubAPI.MLBAPIError` class attribute pattern so
    `except api.MLBAPIError` in controller code catches stub-raised errors.

    Provide a `make_controller_stub_api` pytest factory fixture mirroring Phase 3's
    `make_stub_api`.

    Also provide session-scoped fixture loaders for the three NEW team-stats
    fixtures + pass-through for the existing `team_stats_147_2026.json`:

    - `team_stats_nyy_2026()` → `tests/fixtures/team_stats_147_2026.json` → `roster` list
    - `team_stats_empty()` → `tests/fixtures/team_stats_empty.json` → `roster` list
    - `team_stats_all_pitchers()` → `tests/fixtures/team_stats_all_pitchers.json` → `roster` list
    - `team_stats_zero_hr_player()` → `tests/fixtures/team_stats_zero_hr_player.json` → `roster` list

    Path handling: follow Phase 3's REAL_FIXTURES pattern
    (`Path(__file__).parent.parent / "fixtures"`). Hard-constrain to the fixtures
    directory (T-3-05 mitigation — no `..` traversal).

    **Step 2 — Hand-author 3 synthetic fixtures.**

    Each fixture MUST match the `{"roster": [...]}` shape from the existing
    `team_stats_147_2026.json`. Inspect that file first to mirror field names
    exactly (at minimum: `person.id`, `person.fullName`, `position.type`,
    `person.stats[0].splits[0].stat.homeRuns`).

    1. `tests/fixtures/team_stats_empty.json`:
       ```json
       {"roster": []}
       ```

    2. `tests/fixtures/team_stats_all_pitchers.json`:
       Subset of team_stats_147_2026.json — keep ONLY entries where
       `position.type == "Pitcher"`. If the NYY fixture has fewer than 2 pitchers,
       hand-author 2 synthetic entries with `position.type="Pitcher"`,
       `person.id` in range 900000+, and `person.stats[0].splits[0].stat.homeRuns=0`.

    3. `tests/fixtures/team_stats_zero_hr_player.json`:
       ONE roster entry — `person.id=900001`, `person.fullName="Test Hitter"`,
       `position.type="Outfielder"`,
       `person.stats[0].splits[0].stat.homeRuns=0`. Wrap in `{"roster": [...]}`.

    **Step 3 — No tests in this task.** This task only lands infra; Plan 04-02
    tasks consume these fixtures and the stub via the factory fixture.
  </action>
  <verify>
    <automated>python -c "import json; [json.load(open(f)) for f in ['tests/fixtures/team_stats_empty.json','tests/fixtures/team_stats_all_pitchers.json','tests/fixtures/team_stats_zero_hr_player.json']]; import tests.controller.conftest" &amp;&amp; pytest tests/ -q --collect-only</automated>
  </verify>
  <done>
    conftest.py imports cleanly; all three fixtures are valid JSON with a
    `roster` key; pytest collects `tests/controller/` as a package with no
    collection errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| statsapi.mlb.com → services/mlb_api.py | External API response enters the process; parsed JSON flows inward. |
| Fixtures on disk → tests | Test-only; trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | `_raw_team_hitting_stats` URL construction | mitigate | Interpolate `team_id` and `season` as ints only; reject non-int at wrapper boundary (type hint + defensive `int(...)` cast if needed). Public endpoint, no auth, low SSRF impact, but eliminates injection risk. |
| T-4-02 | Information Disclosure | ViewModel.to_dict() over user-controlled player names | accept | Streamlit auto-escapes strings rendered via `st.json` / `st.dataframe` (no `unsafe_allow_html`). Names come from MLB's trusted feed. |
| T-4-03 | DoS | Pathological empty-roster response forcing repeated fetches | mitigate | `@st.cache_data(ttl="1h")` at the services boundary (D-14) — single HTTP call per team per hour. |
| T-4-04 | Path traversal | Fixture loaders in conftest | mitigate | Hard-constrain to `tests/fixtures/` via `Path(__file__).parent.parent / "fixtures" / name` — bare filenames only, mirrors Phase 3 pattern. |
</threat_model>

<verification>
- `pytest tests/services/test_team_hitting_stats.py -q` → green
- `pytest tests/controller/test_view_model.py -q` → green
- `pytest tests/ -q` → full suite green (Phase 1-3 regression guard)
- `python -c "from mlb_park.pipeline import load_parks; print(load_parks)"` → no ImportError
- No new `streamlit` import in `src/mlb_park/controller.py` (grep check)
</verification>

<success_criteria>
- `get_team_hitting_stats(team_id, season)` is importable from
  `mlb_park.services.mlb_api`, decorated with `@st.cache_data(ttl="1h")`,
  and returns the `roster` list from the D-11-amended endpoint.
- `load_parks` is re-exported via `mlb_park.pipeline` (D-02 single-import-origin).
- `ViewModel` frozen dataclass matches D-06 schema exactly (field names + types).
- `ViewModel.to_dict()` round-trips cleanly through `json.dumps` AND produces
  the RESEARCH.md-prescribed verdict_matrix summary shape
  (`{"shape", "venue_ids", "cleared_per_park"}`).
- `tests/controller/conftest.py` provides a `ControllerStubAPI` factory and
  fixture loaders for all four team-stats fixtures.
- Three new synthetic fixtures exist and are valid JSON with a `roster` root key.
- Full test suite (`pytest tests/ -q`) passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-controller-selectors-ui/04-01-SUMMARY.md`
summarizing: new services wrapper signature + URL, ViewModel schema (copy D-06 block),
`to_dict()` handling of VerdictMatrix (RESEARCH-prescribed summary shape),
`load_parks` re-export, stub API extensions, fixture inventory.
</output>
</content>
</invoke>