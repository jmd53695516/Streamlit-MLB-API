---
phase: 08-cloud-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .streamlit/config.toml
  - src/mlb_park/app.py
  - requirements.txt
  - pyproject.toml
  - data/venues_cache.json
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-04

must_haves:
  truths:
    - ".gitignore excludes only .streamlit/secrets.toml, not all of .streamlit/"
    - ".gitignore does not exclude data/venues_cache.json"
    - ".streamlit/config.toml exists with headless=true, gatherUsageStats=false, and MLB theme colors"
    - "app.py calls st.set_page_config with layout='wide' and page_title='MLB HR Park Explorer' before any other st.* call"
    - "requirements.txt has exactly 4 runtime dependencies and no pytest"
    - "pyproject.toml has pytest under [project.optional-dependencies].dev, not under [project].dependencies"
    - "data/venues_cache.json is tracked by git (not ignored)"
  artifacts:
    - path: ".streamlit/config.toml"
      provides: "Streamlit server, browser, and theme configuration"
      contains: "headless = true"
    - path: ".gitignore"
      provides: "Narrowed exclusions for Cloud compatibility"
      contains: ".streamlit/secrets.toml"
    - path: "requirements.txt"
      provides: "Cloud-ready dependency list without pytest"
    - path: "pyproject.toml"
      provides: "Local dev config with pytest in optional-dependencies"
      contains: "optional-dependencies"
    - path: "data/venues_cache.json"
      provides: "Pre-built venue fence data eliminating cold-start fetch"
  key_links:
    - from: ".streamlit/config.toml"
      to: "Streamlit Cloud runtime"
      via: "Streamlit reads config.toml at startup"
      pattern: "headless = true"
    - from: "src/mlb_park/app.py"
      to: "st.set_page_config"
      via: "First st.* call in module"
      pattern: "set_page_config.*layout.*wide"
    - from: ".gitignore"
      to: "data/venues_cache.json"
      via: "Must NOT block git tracking"
      pattern: "secrets.toml"
---

<objective>
Prepare the repository for Streamlit Community Cloud deployment: create config.toml with MLB theme and server settings, add st.set_page_config() to app.py, narrow .gitignore so config.toml and venues_cache.json are tracked, clean requirements.txt by removing pytest, move pytest to pyproject.toml optional-dependencies, and git-add venues_cache.json.

Purpose: Without these changes, Cloud would use default narrow layout, install pytest unnecessarily, miss the config.toml (gitignored), and cold-start fetch all venue data on first load.
Output: Six files modified/created, all tracked by git, ready for push to main branch in Plan 02.
</objective>

<execution_context>
@C:/Users/joedo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joedo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cloud-deployment/08-CONTEXT.md
@.planning/phases/08-cloud-deployment/08-RESEARCH.md

<interfaces>
<!-- Key integration points the executor needs -->

From src/mlb_park/app.py (current state — NO st.set_page_config call):
```python
# Line 62 — first st.* call in the file currently:
st.title("MLB HR Park Explorer")
```

From src/mlb_park/config.py:
```python
# Line 27 — VENUES_FILE path (must NOT change):
VENUES_FILE = _ROOT / "data" / "venues_cache.json"
```

From .gitignore (current — blocks .streamlit/ and data/ entirely):
```gitignore
.streamlit/
data/
```

From requirements.txt (current — has pytest on line 5):
```
pytest>=8.0,<9.0
```

From pyproject.toml (current — pytest in main dependencies):
```toml
dependencies = [
    ...
    "pytest>=8.0,<9.0",
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow .gitignore, create config.toml, add set_page_config to app.py, track venues cache</name>
  <files>.gitignore, .streamlit/config.toml, src/mlb_park/app.py, data/venues_cache.json</files>
  <read_first>
    - .gitignore (current exclusion rules to narrow)
    - src/mlb_park/app.py (find existing st.set_page_config or confirm absent; identify first st.* call)
    - src/mlb_park/config.py (confirm VENUES_FILE path unchanged at data/venues_cache.json)
    - data/venues_cache.json (confirm file exists and is non-empty)
  </read_first>
  <action>
**Step 1 — Narrow .gitignore (per D-09, D-13):**

Replace the current `.gitignore` with this exact content:

```gitignore
# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Streamlit — track config.toml, ignore secrets only (D-13)
.streamlit/secrets.toml

# OS
.DS_Store
Thumbs.db
```

Key changes: removed `.streamlit/` (was blocking config.toml) and removed `data/` (was blocking venues_cache.json). Added `.streamlit/secrets.toml` as the only Streamlit exclusion.

**Step 2 — Create .streamlit/config.toml (per D-01, D-02, D-04):**

Create directory `.streamlit/` if it does not exist. Write `.streamlit/config.toml` with this exact content:

```toml
[server]
headless = true

[browser]
gatherUsageStats = false

[theme]
base = "light"
primaryColor = "#002D72"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#262730"
```

Do NOT put `layout = "wide"` in config.toml — there is no such key. Layout is controlled by `st.set_page_config()` in app.py.

**Step 3 — Add st.set_page_config to app.py (per D-01, D-03):**

Insert `st.set_page_config(page_title="MLB HR Park Explorer", layout="wide")` as the FIRST `st.*` call in `src/mlb_park/app.py`. It must appear BEFORE the existing `st.title(...)` on line 62. Place it immediately after the imports and callback definitions, before `st.title(...)`. Example insertion point — add a blank line then:

```python
st.set_page_config(page_title="MLB HR Park Explorer", layout="wide")
```

Then keep the existing `st.title("MLB HR Park Factor Explorer")` line as-is (it is the visible heading, distinct from the browser tab title).

**Step 4 — Verify and git-add venues_cache.json (per D-08, D-10):**

Run `git check-ignore -v data/venues_cache.json` to confirm the file is no longer ignored (should return nothing). Verify the file exists, is non-empty (should be several KB with 30 venue entries), and has reasonable content. Then run `git add data/venues_cache.json`.
  </action>
  <verify>
    <automated>cd "C:\Users\joedo\Documents\Streamlit MLB API" && python -c "import tomllib; d=tomllib.load(open('.streamlit/config.toml','rb')); assert d['server']['headless']==True; assert d['browser']['gatherUsageStats']==False; assert d['theme']['primaryColor']=='#002D72'; print('config.toml OK')" && git check-ignore -v data/venues_cache.json; echo "exit:$?" && git check-ignore -v .streamlit/config.toml; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains the line `.streamlit/secrets.toml`
    - .gitignore does NOT contain the line `.streamlit/`
    - .gitignore does NOT contain the line `data/`
    - .streamlit/config.toml contains `headless = true`
    - .streamlit/config.toml contains `primaryColor = "#002D72"`
    - .streamlit/config.toml contains `gatherUsageStats = false`
    - .streamlit/config.toml does NOT contain `layout`
    - src/mlb_park/app.py contains `st.set_page_config(page_title="MLB HR Park Explorer", layout="wide")`
    - src/mlb_park/app.py has `set_page_config` BEFORE `st.title`
    - `git check-ignore -v data/venues_cache.json` returns empty (not ignored)
    - `git check-ignore -v .streamlit/config.toml` returns empty (not ignored)
    - data/venues_cache.json exists and is > 1KB
  </acceptance_criteria>
  <done>
    .gitignore narrowed to exclude only secrets.toml, config.toml created with MLB theme and server settings, app.py has set_page_config as first st.* call, venues_cache.json is git-trackable and staged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean requirements.txt and pyproject.toml for Cloud</name>
  <files>requirements.txt, pyproject.toml</files>
  <read_first>
    - requirements.txt (confirm pytest is on line 5)
    - pyproject.toml (confirm pytest is in [project].dependencies, check for existing optional-dependencies section)
  </read_first>
  <action>
**Step 1 — Remove pytest from requirements.txt (per D-05):**

Write `requirements.txt` with exactly these 4 lines (no trailing newline issues):

```
streamlit>=1.55,<2.0
requests>=2.32,<3.0
plotly>=6.7,<7.0
pandas>=2.2,<3.0
```

**Step 2 — Move pytest to optional-dependencies in pyproject.toml (per D-06):**

Write `pyproject.toml` with this exact content:

```toml
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mlb-park"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.55,<2.0",
    "requests>=2.32,<3.0",
    "plotly>=6.7,<7.0",
    "pandas>=2.2,<3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0,<9.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
```

Key changes from current: removed `"pytest>=8.0,<9.0"` from `[project].dependencies`, added new `[project.optional-dependencies]` section with `dev = ["pytest>=8.0,<9.0"]`.

**Step 3 — Verify existing tests still pass:**

Run `pytest tests/ -x -q` to confirm the test suite is unaffected by the config changes.
  </action>
  <verify>
    <automated>cd "C:\Users\joedo\Documents\Streamlit MLB API" && grep -c pytest requirements.txt | grep "^0$" && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'pytest>=8.0,<9.0' not in d['project']['dependencies']; assert 'pytest>=8.0,<9.0' in d['project']['optional-dependencies']['dev']; print('pyproject.toml OK')" && pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains exactly 4 lines (streamlit, requests, plotly, pandas)
    - requirements.txt does NOT contain the string `pytest`
    - pyproject.toml `[project].dependencies` does NOT contain `pytest`
    - pyproject.toml contains `[project.optional-dependencies]` section
    - pyproject.toml contains `dev = ["pytest>=8.0,<9.0"]` under optional-dependencies
    - pyproject.toml still contains `[tool.pytest.ini_options]` and `[tool.setuptools.packages.find]`
    - `pytest tests/ -x -q` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>
    requirements.txt has 4 runtime deps only (no pytest), pyproject.toml has pytest under [project.optional-dependencies].dev, all existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser -> Streamlit Cloud | Public app, no auth, read-only MLB data display |
| Streamlit Cloud -> statsapi.mlb.com | Public API, no API key, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | I (Info Disclosure) | .streamlit/secrets.toml | mitigate | D-13: .gitignore excludes .streamlit/secrets.toml to prevent accidental commit of any future secrets |
| T-08-02 | T (Tampering) | requirements.txt | accept | Public app with no sensitive data; supply chain risk is standard for any pip-installed app |
| T-08-03 | I (Info Disclosure) | data/venues_cache.json | accept | Contains only public MLB venue dimensions — no PII, no secrets |
</threat_model>

<verification>
1. `.streamlit/config.toml` parses without error and contains all required keys
2. `git check-ignore` confirms neither `config.toml` nor `venues_cache.json` are ignored
3. `app.py` has `set_page_config` before any other `st.*` call
4. `requirements.txt` has no pytest line
5. `pyproject.toml` has pytest only in optional-dependencies
6. Full test suite passes: `pytest tests/ -x -q`
</verification>

<success_criteria>
Repository is Cloud-ready: config.toml committed with MLB theme, app.py has wide layout + tab title, requirements.txt is clean, venues_cache.json is tracked, and all 110+ tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cloud-deployment/08-01-SUMMARY.md`
</output>
